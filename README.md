# spam_detector
Spam Detector is a machine learning-based system that classifies messages as spam or ham using NLP techniques. It preprocesses text, extracts features (TF-IDF/BoW), and applies models like Naive Bayes or Logistic Regression to ensure accurate and efficient spam detection.
